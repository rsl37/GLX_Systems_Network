# GLX Civic Networking App - Production Environment Configuration
# This file contains production-ready defaults for the GLX platform
# Copy and customize these values for your production deployment

# =============================================================================
# CORE APPLICATION SETTINGS
# =============================================================================

# Production environment mode - enables all security features
NODE_ENV=production

# Default port for production (Vercel overrides this dynamically)
PORT=3000

# Data directory for uploads and database
DATA_DIRECTORY=./data

# =============================================================================
# AUTHENTICATION & SECURITY (GENERATE SECURE VALUES)
# =============================================================================

# JWT signing key - MUST be changed for production
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_THIS_TO_SECURE_64_CHARACTER_HEX_STRING_FOR_PRODUCTION_USE

# JWT refresh token signing key - MUST be changed for production
# Generate with: openssl rand -hex 32
JWT_REFRESH_SECRET=CHANGE_THIS_TO_SECURE_64_CHARACTER_REFRESH_TOKEN_SECRET

# Master encryption key for sensitive data - MUST be changed for production
# Generate with: openssl rand -hex 32
ENCRYPTION_MASTER_KEY=CHANGE_THIS_TO_SECURE_64_CHARACTER_ENCRYPTION_MASTER_KEY

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Production database URL - RECOMMENDED: Use PostgreSQL for production
# For Vercel: Use Vercel Postgres connection string
# For other hosting: postgres://user:password@host:port/database
# For development/testing: sqlite:./data/database.sqlite (auto-fallback)
# Leave empty to use SQLite fallback (not recommended for production)
DATABASE_URL=sqlite:./data/database.sqlite

# =============================================================================
# CORS & CLIENT CONFIGURATION
# =============================================================================

# Production frontend URL - UPDATE TO YOUR DOMAIN
CLIENT_ORIGIN=https://glxcivicnetwork.me

# Frontend URL (legacy support) - UPDATE TO YOUR DOMAIN
FRONTEND_URL=https://glxcivicnetwork.me

# Trusted origins (comma-separated) - UPDATE TO YOUR DOMAINS
TRUSTED_ORIGINS=https://glxcivicnetwork.me,https://glx-civic-networking.vercel.app

# =============================================================================
# WEBSOCKET CONFIGURATION
# =============================================================================

# Socket.IO path for WebSocket connections
SOCKET_PATH=/socket.io

# =============================================================================
# EMAIL CONFIGURATION (PRODUCTION SMTP)
# =============================================================================

# Production SMTP settings - CONFIGURE FOR YOUR EMAIL PROVIDER
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-production-email@domain.com
SMTP_PASS=your-production-app-password
SMTP_FROM=noreply@glxcivicnetwork.me

# =============================================================================
# SMS/PHONE VERIFICATION (PRODUCTION TWILIO)
# =============================================================================

# Production Twilio configuration - CONFIGURE FOR YOUR TWILIO ACCOUNT
TWILIO_SID=your-production-twilio-account-sid
TWILIO_AUTH_TOKEN=your-production-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================

# 1. SECURITY: All secret keys MUST be changed from defaults before deployment
# 2. DATABASE: PostgreSQL is strongly recommended for production
# 3. DOMAINS: Update all URL variables to match your actual domain
# 4. SMTP: Configure real email service for production notifications
# 5. TWILIO: Configure real SMS service for phone verification
# 6. SECRETS: Never commit actual secrets to version control
# 7. MONITORING: Consider adding monitoring and analytics keys
# 8. BACKUP: Ensure regular database backups are configured