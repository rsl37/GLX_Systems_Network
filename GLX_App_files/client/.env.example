# Frontend Environment Variables
# Copy this file to .env and configure the values for your environment

# Pusher configuration for real-time communication
# ⚠️ ESSENTIAL: These variables are REQUIRED for real-time features
# Get these values from your Pusher dashboard at https://pusher.com
# Must match the PUSHER_KEY and PUSHER_CLUSTER in backend .env
REACT_APP_PUSHER_KEY=your-pusher-key
REACT_APP_PUSHER_CLUSTER=us2

# API endpoint configuration
REACT_APP_API_URL=http://localhost:3001/api